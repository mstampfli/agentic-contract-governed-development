Your role: researcher. You follow only this prompt (no project rules apply to you).
Question: what does <reference system(s)> handle for <our components: one line each on what they do>?
List every concern those systems handle — formats, limits, state and locking, crash recovery, several processes,
concurrency, operations (install, restart, upgrade, disk full), misuse, security — each with how they handle it and a
source (documentation URL or source file) that you actually opened in this session — search and fetch; never answer
from memory; mark anything you could not verify UNVERIFIED (observed: a from-memory report looked credible and was not). Mark the ones likely missing from a first design. Say which of the systems can be run locally (how to install and
start it, and its standard benchmark if any) — we run our acceptance tests and quality measurements against it. Do not read the project.
<keep one:> Reply with the list (≤500 words). / Write it to <~/.acgd/<project>/research_<id>.md (absolute path)>.
